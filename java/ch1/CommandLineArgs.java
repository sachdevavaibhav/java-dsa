package ch1;
public class CommandLineArgs {
    public static void main(String[] args) {
        System.out.println(args[0]); //the parameter args (can be named anything) in main method receives arguments from the command line in form of array of type string.
    }
}